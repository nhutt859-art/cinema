server:
  port: 8080

spring:
  application:
    name: cinema-be

  datasource:
    url: jdbc:postgresql://ep-shy-mouse-aovpduzv.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_7Cgx5pasXwet
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000

  sql:
    init:
      continue-on-error: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database: postgresql
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Ho_Chi_Minh
    open-in-view: false

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 16
          max-idle: 8
          min-idle: 4

  mail:
    host: smtp.gmail.com
    port: 587
    username: nhutt859@gmail.com
    password: ysab nmia dlnv zzbs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        transport:
          protocol: smtp

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:your-google-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:your-google-client-secret}
            scope:
              - email
              - profile
          facebook:
            client-id: ${FACEBOOK_CLIENT_ID:your-facebook-client-id}
            client-secret: ${FACEBOOK_CLIENT_SECRET:your-facebook-client-secret}
            scope:
              - email
              - public_profile

app:
  jwt:
    secret: ${JWT_SECRET:O3H07Jx+UOlQP0HSrW346hj71UDxyF/kfXpGSyZSy/Y=}
    access-token-expiration: 3600000
    refresh-token-expiration: 604800000
  redis:
    enabled: false
    seat-lock-ttl: 900
    max-seats-per-booking: 8
  cors:
    allowed-origins: http://localhost:5173,http://localhost:3000

logging:
  level:
    com.cinema: DEBUG
    org.springframework.security: INFO
