spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CinemaDB
    username: postgres
    password: 29072009tara
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Ho_Chi_Minh
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      continue-on-error: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:nhutt859@gmail.com}
    password: ${MAIL_PASSWORD:ysab nmia dlnv zzbs}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        transport:
          protocol: smtp

app:
  redis:
    enabled: false
    seat-lock-ttl: 900
    max-seats-per-booking: 8

logging:
  level:
    com.cinema: DEBUG
    org.hibernate.SQL: DEBUG
